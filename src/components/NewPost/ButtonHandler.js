import "./ButtonHandler.css";

const ButtonHandler = () => {
  return (
    <div id="mobile-buttons-handler">
      <button className="mobile-buttons active">Editor</button>
      <button className="mobile-buttons">Preview</button>
    </div>
  );
};

export default ButtonHandler;
